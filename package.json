{
  "name": "@dman01989/js-check",
  "version": "0.1.1",
  "description": "",
  "main": "dist/index.mjs",
  "bin": {
    "js": "./dist/index.mjs"
  },
  "engines": {
    "node": ">=14.16.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "dev": "rm -rf dist && tsc && node ./dist/index.mjs",
    "test-global": "rm -rf dist && tsc && npm remove -g js; npm link"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "chalk": "^5.1.2"
  }
}
